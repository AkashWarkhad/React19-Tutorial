import React, { Component} from "react"

export default class ClassComponent extends Component 
{
    render()
    {
        return <h4>Welcome to react component</h4>
    }
}
  // export default ClassComponent; // Can do this also if we remove export default from class.